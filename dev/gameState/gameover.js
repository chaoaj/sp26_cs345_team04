function drawGameOver() {
  background(0);

  fill(255);

  textSize(50);
  text("GAME OVER", width / 2 - 270 / 2, height / 2);
  
  textSize(20);
  text("Press ESC to Return to Level Select", width / 2 - 420 / 2, height / 2 + 50);
}